name: FTP Deploy (Multiple Projects)

on:
  push:
    branches:
      - master  # Sadece master branch'e push yapıldığında çalışır

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Kodu GitHub'dan Çek
      - name: Kodu GitHub'dan Çek
        uses: actions/checkout@v2

      # 📌 .NET 8 SDK Kurulumu (MyBlogSite İçin)
      - name: .NET 8 SDK Kurulumu
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      # 📌 İlk Proje: MyBlogSite (.NET 8)
      - name: MyBlogSite Projesini Build ve Publish Et (.NET 8)
        run: dotnet publish ./MyBlogSite.APIs/MyBlogSite/MyBlogSite.csproj -c Release -o ./publish-myblogsite

      # 📌 Publish Klasörü Kontrolü (MyBlogSite)
      - name: MyBlogSite Publish Klasörü Kontrolü
        run: ls -la ./publish-myblogsite || echo "MyBlogSite için publish başarısız!"

      # MyBlogSite İçin web.config Güncelleme
      - name: MyBlogSite Web.config İçine ASPNETCORE_ENVIRONMENT Ekle
        run: |
          if [ -f "./publish-myblogsite/web.config" ]; then
            sed -i 's|</aspNetCore>|<environmentVariables><environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Production" /></environmentVariables></aspNetCore>|' ./publish-myblogsite/web.config
          fi

      # 📌 .NET 9 SDK Kurulumu (NotificationConsumer İçin)
      - name: .NET 9 SDK Kurulumu
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.0.x'

      # 📌 İkinci Proje: NotificationConsumer (.NET 9)
      - name: NotificationConsumer Projesini Build ve Publish Et (.NET 9)
        run: dotnet publish ./MyBlogSite.Consumers/NotificationConsumer/NotificationConsumer.csproj -c Release -o ./publish-notificationconsumer

      # 📌 Publish Klasörü Kontrolü (NotificationConsumer)
      - name: NotificationConsumer Publish Klasörü Kontrolü
        run: ls -la ./publish-notificationconsumer || echo "NotificationConsumer için publish başarısız!"

      # NotificationConsumer İçin web.config Güncelleme
      - name: NotificationConsumer Web.config İçine ASPNETCORE_ENVIRONMENT Ekle
        run: |
          if [ -f "./publish-notificationconsumer/web.config" ]; then
            sed -i 's|</aspNetCore>|<environmentVariables><environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Production" /></environmentVariables></aspNetCore>|' ./publish-notificationconsumer/web.config
          fi

      # 📌 FTP 1: MyBlogSite'ı Yükle (clean yerine dangerous-clean-slate kullanılıyor)
      - name: MyBlogSite Kodlarını FTP 1'e Yükle
        uses: SamKirkland/FTP-Deploy-Action@4.3.1
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./publish-myblogsite/  # Sadece MyBlogSite için publish edilen dosyaları yükle
          server-dir: /httpdocs/  # FTP sunucusundaki hedef klasör
          dangerous-clean-slate: true  # Eski dosyaları temizleyerek yükleme yap

      # 📌 FTP 2: NotificationConsumer'ı Yükle (clean yerine dangerous-clean-slate kullanılıyor)
      - name: NotificationConsumer Kodlarını FTP 2'ye Yükle
        uses: SamKirkland/FTP-Deploy-Action@4.3.1
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./publish-notificationconsumer/  # Sadece NotificationConsumer için publish edilen dosyaları yükle
          server-dir: /mail-consumer.analizgaraj.com/  # FTP sunucusundaki hedef klasör
          dangerous-clean-slate: true  # Eski dosyaları temizleyerek yükleme yap
